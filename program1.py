class Solution:
   
    def getTotalIsles(self, grid: list[list[str]]) -> int:
    #    write your code here
        r, c=len(grid), len(grid[0])
        
        for i in range(r):
            for j in range(c):
                #  top check
                if i==0:
                    pass
                # left check
                if j=0:
                    pass
                # right check
                # bottom check

                    
        return 0