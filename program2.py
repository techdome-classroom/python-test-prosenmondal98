def decode_message( s: str, p: str) -> bool:

# write your code here
        for i in range(len(s)):
                for j in range(len(p)):
                        if 
                        elif s[i]==p[j] or 'j':
                                continue
                        
                        else:
                                return False
                        

  
        return False